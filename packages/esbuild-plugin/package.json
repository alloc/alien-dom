{
  "name": "@alien-dom/esbuild",
  "version": "0.1.1",
  "type": "module",
  "main": "dist/plugin.js",
  "types": "dist/plugin.d.ts",
  "scripts": {
    "build": "rimraf dist && tsup src/plugin.ts --format esm --dts",
    "dev": "tsup src/plugin.ts --format esm --dts --watch --sourcemap"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/node": "^18.15.11",
    "esbuild": "^0.17.17"
  },
  "dependencies": {
    "@alien-dom/nebu": "^0.1.6",
    "esbuild-extra": "^0.1.9",
    "nebu": "^2.3.8"
  },
  "peerDependencies": {
    "esbuild": "*"
  }
}
